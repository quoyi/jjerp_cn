<%= form_for(@order_category) do |f| %>
  <% if @order_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_category.errors.count, "error") %> prohibited this order_category from being saved:</h2>

      <ul>
      <% @order_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
