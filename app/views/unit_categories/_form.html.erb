<%= form_for(@unit_category) do |f| %>
  <% if @unit_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unit_category.errors.count, "error") %> prohibited this unit_category from being saved:</h2>

      <ul>
      <% @unit_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
