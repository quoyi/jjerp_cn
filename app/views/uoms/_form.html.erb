<%= form_for(@uom) do |f| %>
  <% if @uom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uom.errors.count, "error") %> prohibited this uom from being saved:</h2>

      <ul>
      <% @uom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :val %><br>
    <%= f.text_field :val %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_field :note %>
  </div>
  <div class="field">
    <%= f.label :deleted %><br>
    <%= f.check_box :deleted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
