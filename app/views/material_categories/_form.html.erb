<%= form_for(@material_category) do |f| %>
  <% if @material_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@material_category.errors.count, "error") %> prohibited this material_category from being saved:</h2>

      <ul>
      <% @material_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
